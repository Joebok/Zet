# To Do items

## World-Position

ChatGPT Plus





Yesterday 5:55 PM
image(447).png
image(448).png
image(449).png
image(450).png
image(451).png
image(452).png
image(453).png
# Render Task

Create one finished scene. Do not show the planning grid or split the image into comic panels.

# Story Beat

- Tsaeytte wants to get out of hell as quickly as possible, and that means jumping down the pit. The rest of the party have discovered a door in the far wall and are concerned that Tsaeytte is going to fall into the pit.

# Reference Image Assignment

- Hell-Stairs - {{AUX:place:hell-stairs:stairs}}
  Preserve architecture, structural design, identifying materials, and location-defining features.
  Ignore source camera composition, framing, people, lighting, weather, and temporary objects.
- Tsaeytte - {{ASSET:Tsaeytte:Adult:28:Costume | Left-Profile | Canonical Adventure Gear}}
  Preserve identity, facial features, hair, ears if applicable, body proportions, costume design, costume colors, and signature worn items.
  Ignore source pose, expression, action, camera angle, framing, background, and lighting.
- Freydis - {{AUX:person:freydis:freydis}}
  Preserve identity, facial features, hair, ears if applicable, and body proportions.
  Ignore source costume unless explicitly assigned, pose, expression, action, camera angle, framing, background, and lighting.
- Galen - {{AUX:person:galen:galen}}
  Preserve identity, facial features, hair, ears if applicable, and body proportions.
  Ignore source costume unless explicitly assigned, pose, expression, action, camera angle, framing, background, and lighting.
- Peri - {{AUX:person:peri:peri}}
  Preserve identity, facial features, hair, ears if applicable, and body proportions.
  Ignore source costume unless explicitly assigned, pose, expression, action, camera angle, framing, background, and lighting.
- Rin - {{AUX:person:rin:rin}}
  Preserve identity, facial features, hair, ears if applicable, and body proportions.
  Ignore source costume unless explicitly assigned, pose, expression, action, camera angle, framing, background, and lighting.
- Hell-Tusk - {{AUX:thing:hell-tusk:hell-tusk}}
  Preserve identity, facial features, hair, ears if applicable, and body proportions.
  Ignore source costume unless explicitly assigned, pose, expression, action, camera angle, framing, background, and lighting.

# Canvas

- Landscape 16:9.

# Composition

- Primary focal point: Tsaeytte.
- From left to right the viewer sees: Tsaeytte, then Freydis, then Galen, then Rin, then Peri.
- Tsaeytte stands alone at the edge of the pit. The remaining four party members stand clustered near the doorway in the far wall calling out to her. The remaining four party members stand clustered near the doorway in the far wall, calling out to Tsaeytte.

# Backdrop and Setting

- The scene takes place in one of the levels of hell.
- Hell-Stairs defines the overall background and surrounding setting.
- Show Show stone steps spiraling down from an opening in the ceiling toward a central bottomless pit. The floor is stone. The outer edges of the chamber fade into smoky darkness.
- Integrate all characters, props, and action naturally within this setting.

# Character and Object Staging

- **Tsaeytte:** At the edge of the pit. Looking straight down into its depths in the center foreground. Expression: excited.
- **Freydis:** Standing near doorway in the right distant background. Looks directly at Tsaeytte. Expression: alarm.
- **Galen:** Standing near doorway in the right distant background. Looks directly at Tsaeytte. Expression: frustrated.
- **Peri:** Turning back over shoulder in the right distant background. Looks directly at Tsaeytte. Expression: dismay.
- **Rin:** Standing near doorway in the right distant background, two hands raised in warning. Looks directly at Tsaeytte. Expression: concern, alarm.
- **Hell-Tusk:** Stands in the center foreground.

# Lighting and Mood

- Lighting: dark and smoky.
- Mood: alarm and excitement.
- Atmosphere: air is thick with smoke and occasional sparks of fire.
- Art style: Painterly semi-realistic fantasy illustration with anime-influenced facial proportions, large expressive eyes, refined linework, and warm storybook-fantasy color handling.

# Dialogue Panels

## Dialogue Panel 1

Rin says exactly: "no!"
Place the panel so the dialogue reads as directed toward Tsaeytte.
Aim the dialogue-panel pointer at speaker mouth.
Wrap the dialogue in no more than 1 line.
- **Special instructions:** small text and dialog panel to indicate distance

## Dialogue Panel 2

Freydis says exactly: "don't jump!"
Place the panel so the dialogue reads as directed toward Tsaeytte.
Aim the dialogue-panel pointer at speaker mouth.
Wrap the dialogue in no more than 3 lines.
- **Special instructions:** small text and dialog panel to indicate distance

## Dialogue Panel 3

Tsaeytte says exactly: "It's the quickest way down!"
Place the panel so the dialogue reads as directed toward Tsaeytte.
Aim the dialogue-panel pointer at speaker mouth.
Wrap the dialogue in no more than 2 lines.

# Scene Element Preservation

- Preserve the story's canonical art style across all scene images.
- Preserve recurring characters
- Costumes
- Props
- And locations when referenced by scene tags.

## Hell-Stairs

**Location design:** Show stone steps spiraling down from an opening in the ceiling toward a central bottomless pit. The floor is stone. The outer edges of the chamber fade into smoky darkness.

**Element Override:** The stairs and the bottomless pit are on the left half of the image. The right half is stone floor merging into shadowy, indistinct walls. The doorway occupied by the party is visible on the far right wall.

## Tsaeytte

**Identity:** Petite young-adult high elf with a soft heart-shaped face, delicate pointed chin, fair skin, large expressive violet eyes, and long pointed ears. Her thick black hair forms a tousled chin-length bob with inward-curving ends framing both cheeks. Preserve her lithe, graceful build and recognizable facial identity from the reference image.

**Costume - Canonical Adventure Gear:** Teal off-shoulder ruffled crop top with short upper-arm puff sleeves, exposed midriff, and broad brown leather belt. Deep teal wool overskirt worn over dark fitted leggings. The overskirt has a permanently open front. Its hem is shortest at the front, reaches knee height at the sides, and tapers to ankle height at the center back, forming a smooth continuous silhouette in every view. Tall fitted warm brown leather boots, with a single folded cuff at the top, and high block heels. Preserve the blue-violet pendant, earrings, and the canonical teal-and-brown sorceress silhouette.

**Element Override:** Facing left profile. Leaning dangerously over the edge while pointing emphatically into the pit. She carries the Hell-Tusk by its tapered end, dragging the thick base behind her across the stone floor.

## Freydis

**Identity:** Freydis — Tall, powerfully muscular human barbarian with a rugged, battle-hardened physique. Fair skin marked by dirt, scars, and weathering. Short asymmetrical platinum-blonde hair with one side closely shaved and the longer side swept dramatically across the forehead. Intense gray-blue eyes, strong angular features, and a stern, unyielding expression. Carries herself with fierce confidence, raw physical strength, and the presence of an experienced warrior.

**Costume:** Freydis Costume — Practical barbarian war gear made from worn leather, fur, bone, and hide. Weathered leather crop top with crossed harness straps, layered ragged hide skirt, broad utility belt carrying pouches, knives, trophies, and survival gear, heavy leather bracers trimmed with fur, sturdy boots, and a dark crimson fur-lined cloak fastened at one shoulder. Bone necklace worn prominently at the neck. Always carries her signature oversized battle axe, featuring a massive cracked stone blade with a large blue eye embedded in the head, heavy spiked metal fittings, and a skull-adorned haft. The axe is an iconic part of her silhouette and should almost always accompany her.

## Galen

**Identity:** Galen is a grumpy, taciturn dwarf with a dark reddish-orange braided beard.

**Costume:** Galen carries two shields and wears plate mail with a helmet.

**Element Override:** Galen is wearing one of the shields on his back. He is holding his forehead in the palm of his free hand.

## Peri

**Identity:** Peri is an elf, possibly with drow ancestry. His skin has a grayish pallor. He is shy and avoids scrutiny as much as possible.

**Costume:** Peri carries 2 hand crossbows and is almost always cloaked and hooded.

## Rin

**Identity:** A dragonborn female of blue dragon ancestry, comely by dragonborn standards. A warlock.

**Costume:** Rin carries an obsidian trident and wears ornate off-white robes with gold trim and subtle electricity symbols.

**Element Override:** Trident is on her back, not in hand. She has taken a step towards Tsaeytte.

## Hell-Tusk

**Element Override:** A massive, nearly straight keratin tusk approximately 5½ feet long. It is as long as a quarterstaff, thick at the base, gradually tapering to a sharp point. The surface is covered with natural growth ridges and rings. Only the final few inches curve slightly.

# Avoid

No inconsistent character identity, wrong costume, modern objects, unreadable faces, merged characters, extra limbs, malformed hands.

# Final Verification

- Character count and identities match the scene JSON.
- Left/right placement and depth match placements.
- Hands, props, gaze, and interactions are readable.
- Setting, lighting, mood, and art style match the source data.

Generated image: Defiant elf at the fiery pit


Edit


This is noticeably cleaner. Removing the independent placement for the Utility Tusk eliminates the biggest contradiction. I think you're close. Here are the remaining issues I see, in order of importance.

1. Duplicate sentence
Composition contains this twice:

The remaining four party members stand clustered near the doorway in the far wall calling out to her.

followed immediately by

The remaining four party members stand clustered near the doorway in the far wall, calling out to Tsaeytte.

Delete one.

2. Typo
Backdrop:

Show Show stone steps...

Delete the duplicated "Show".

3. Tsaeytte position is slightly contradictory
You currently have

At the edge of the pit.

then

Looking straight down into its depths in the center foreground.

The "center foreground" refers to camera position while "at the edge of the pit" refers to world position.

I'd simply write

Standing alone at the edge of the pit, looking straight down into its depths.

The composition section already makes her the focal point.

4. Utility Tusk wording
This is much better than before.

However you currently have

dragging the thick base behind her across the stone floor

and later

letting the base rest on the floor.

These aren't contradictory, but they're slightly different descriptions.

I'd merge them.

Example:

She holds the Utility Tusk near its tapered end, letting the thick broken base drag across the stone floor behind her.

Now there is only one interpretation.

5. "Dangerously"
Current:

Leaning dangerously over the edge...

"Dangerously" is subjective.

Visual models usually respond better to geometry.

Something like

Leaning forward over the edge while pointing emphatically into the pit.

is usually more stable.

6. Galen
Current:

He is holding his forehead in the palm of his free hand.

This is fine.

I'd only shorten to

One shield is strapped to his back. His free hand covers his forehead in exasperation.

It describes the pose more visually.

7. Rin
Current:

Trident is on her back

I'd make it

Trident is strapped across her back.

That usually prevents floating weapons.

8. Utility Tusk identity
Excellent.

One suggestion:

approximately 5½ feet long. About the length of a quarterstaff.

The phrase "quarterstaff" tends to be interpreted as a weapon. Giving the length comparison after the measurement sometimes improves consistency.

9. Story Beat
Current

jumping down the pit

Natural English would be

jumping into the pit

or

jumping down into the pit.

Minor, but it reads better.

10. Tsaeytte's expression
You have

Expression: excited.

and later

pointing emphatically

and later

"It's the quickest way down!"

That should work well.

I might make it

Expression: excited and confident.

It reinforces that she isn't afraid.

Overall
I think this version is about 9.8/10.

The prompt is now internally consistent. The remaining failures you'll likely see are limitations of the image model rather than contradictions in the prompt.

The only thing I'd still consider "worth fixing" before calling it done is consolidating the Utility Tusk description into a single sentence and removing the duplicate sentence and typo. Those are small edits, but they reduce opportunities for the model to wander.


On the issue of Tsaeytte's position, you wrote:

3. Tsaeytte position is slightly contradictory
You currently have
At the edge of the pit.
then
Looking straight down into its depths in the center foreground.
The "center foreground" refers to camera position while "at the edge of the pit" refers to world position.

The way the compiler works is that it takes the <pose> text ("at the edge of the pit. Looking straight down into its depths") and then appends the placement <position> <depth> ("center" "foreground"). This works for all the other placements - can you suggesting wording that would fit into that pattern to resolve the issue? For example, if the <pose> were just "looking straight down into the depths of the pit" and we put the standing at the edge of the pit somewhere else? Or do we want to add another field to the Placement block to which we can add or override the camera-based position with a world position?

I actually think you've stumbled onto something important about the compiler architecture.

The problem isn't your wording. It's that you're currently overloading placement to mean two different things:

camera composition

world location

Those are independent concepts.

For most characters, they happen to correlate:

foreground → physically closer to camera

background → physically farther away

But Tsaeytte exposes the flaw because:

she is in the foreground

she is also at the edge of the pit

Those are not alternatives.

I would not put "at the edge of the pit" in the pose
A pose should ideally be something that could be performed anywhere.

Good pose:

leaning forward while looking into the pit

or

pointing excitedly downward

Those describe the body.

This is not really pose:

at the edge of the pit

That's staging.

I would add a World Position field
For example:

Placement

position: center
depth: foreground
world_position: edge of pit
Then the compiler could emit something like

Standing at the edge of the pit.

before the pose.

Then emit

Leaning forward while looking into the darkness below.

Now there is no ambiguity.

Even better: distinguish composition from staging
I think your scene JSON is naturally evolving into three concepts.

Composition
screen_x: center
depth: foreground
This is purely camera.

World Position
world_position: edge of pit
or

world_position: beside doorway
or

world_position: halfway down stairs
This tells the renderer where the character exists inside the environment.

Pose
Leaning forward while pointing into the pit.
Pure body language.

This scales much better
Suppose later you have

Freydis standing halfway down the stairs.

You don't want to write

Standing halfway down the stairs, looking toward Tsaeytte

inside the pose.

Likewise

Galen beside the wagon

Peri behind a pillar

Rin inside the doorway

These are all world positions, not poses.

If you don't want another field...
If you're trying to minimize JSON changes, I'd move the world position into the Element Override instead.

For example:

Element Override:

Standing at the edge of the pit.
Facing right profile.
Leaning forward while pointing emphatically into the pit.
She holds...
Then the placement block stays purely

position: center
depth: foreground
which is exactly what those fields actually describe.

My recommendation
I would introduce a dedicated field:

Placement
{
    position: center
    depth: foreground
    world_position: edge of pit
}
because I think you'll use it constantly.

Every environment you've shown recently has meaningful locations:

edge of pit

doorway

arch

top of stairs

base of stairs

beside table

behind wagon

next to tree

inside classroom

beside laboratory bench

Those aren't poses, and they aren't camera positions. They describe where an element lives within the scene. Separating them will make both the compiler output and the generated prompts cleaner and more consistent.


Please create a markdown file with CODEX instructions to add the world_position field to the placement block and how to emit it in the final_image_prompt, including how it should interact with the existing "Element <pose> in the <position> <depth> region" camera direction.

CODEX_Add_World_Position_To_Placements.md 


Library
/
CODEX_Add_World_Position_To_Placements.md


# CODEX Instructions: Add `world_position` to Scene Placements

## Goal

Add an optional `world_position` field to each scene placement.

This field describes where an element is located within the fictional environment, independently of its camera-based placement.

Examples:

- `at the edge of the pit`
- `inside the doorway`
- `beside the laboratory table`
- `halfway down the stairs`
- `behind the wagon`
- `under the arch`

The existing placement fields continue to describe the element's position in the image:

- `position`: horizontal screen region, such as `left`, `center`, or `right`
- `depth`: camera depth, such as `foreground`, `midground`, or `background`

The new field must not replace or alter these existing fields.

---

## Conceptual Separation

Treat placement as three distinct concepts:

### Camera placement

Describes where the element appears in the rendered image.

```json
{
  "position": "center",
  "depth": "foreground"
}
```

Compiler output:

```text
Tsaeytte in the center foreground region.
```

### World position

Describes where the element exists within the scene environment.

```json
{
  "world_position": "at the edge of the pit"
}
```

Compiler output:

```text
World position: at the edge of the pit.
```

### Pose

Describes body position, orientation, gesture, or action.

```json
{
  "pose": "Looking straight down into the depths of the pit."
}
```

Compiler output:

```text
Pose: Looking straight down into the depths of the pit.
```

Do not treat `world_position` as pose text and do not merge it into the camera-placement fields in stored scene data.

---

## Schema Change

Add the following optional property to the placement block:

```json
{
  "world_position": "string"
}
```

Recommended placement shape:

```json
{
  "element_id": "tsaeytte",
  "position": "center",
  "depth": "foreground",
  "world_position": "at the edge of the pit",
  "pose": "Looking straight down into the depths of the pit."
}
```

Requirements:

- `world_position` is optional.
- Existing scene files without the field must remain valid.
- Empty strings and whitespace-only values should be treated as absent.
- Do not assign a default world position.
- Do not infer a world position from `position`, `depth`, or `pose`.
- Preserve the exact user-authored text except for trimming leading and trailing whitespace.
- Do not automatically add a preposition such as `at`, `in`, `beside`, or `near`.
- The stored value should be a short spatial phrase, not a full camera instruction.

---

## UI Change

Add an editable field to the Placement block:

```text
World Position
```

Suggested helper text:

```text
Location within the scene, such as "at the edge of the pit" or "inside the doorway."
```

Behavior:

- Leave blank when no world-relative location is needed.
- Save the value directly to `placements[].world_position`.
- Load and display the value when reopening a scene.
- Do not populate it from the pose field.
- Do not modify existing `position` and `depth` controls.

Place it near the existing placement controls, preferably in this order:

1. Position
2. Depth
3. World Position
4. Pose
5. Expression
6. Motion or interaction fields

---

## Final Image Prompt Emission

The current compiler emits placement direction in a form similar to:

```text
Element <pose> in the <position> <depth> region.
```

Update this logic so `world_position` is emitted as a separate world-space clause while preserving the existing camera-space direction.

### Required semantic order

Emit information in this order:

1. Element name
2. World position, when present
3. Pose
4. Camera placement

Recommended final form:

```text
<Element>: <world_position>. <pose> Place <Element> in the <position> <depth> region.
```

Example:

```text
Tsaeytte: At the edge of the pit. Looking straight down into its depths. Place Tsaeytte in the center foreground region.
```

This makes the distinction explicit:

- `at the edge of the pit` is world-space staging
- `looking straight down into its depths` is pose/action
- `center foreground region` is camera composition

---

## Interaction with Existing Camera Direction

The existing camera instruction must remain authoritative for screen placement:

```text
Place <Element> in the <position> <depth> region.
```

`world_position` must supplement this instruction, not replace it.

For example:

```json
{
  "position": "right",
  "depth": "background",
  "world_position": "inside the doorway",
  "pose": "Raising both hands in warning."
}
```

Emit:

```text
Rin: Inside the doorway. Raising both hands in warning. Place Rin in the right background region.
```

Do not emit:

```text
Rin inside the doorway background.
```

Do not emit:

```text
Rin in the right doorway.
```

Do not merge world-space and camera-space location into a single ambiguous phrase.

---

## Preferred Prompt Template

Use this structure for each element staging entry:

```text
- **<Element Name>:**
  World position: <world_position>.
  Pose: <pose>.
  Camera placement: <position> <depth>.
```

Example:

```text
- **Tsaeytte:**
  World position: at the edge of the pit.
  Pose: looking straight down into its depths.
  Camera placement: center foreground.
```

This labeled form is preferred when the final prompt already uses structured markdown sections.

If the compiler currently emits compact single-line staging, use:

```text
- **Tsaeytte:** At the edge of the pit. Looking straight down into its depths. Place Tsaeytte in the center foreground region.
```

Do not emit both the labeled and compact forms.

---

## Capitalization and Punctuation

Normalize only for prompt readability.

Rules:

- Trim leading and trailing whitespace.
- Preserve internal wording.
- Capitalize the first emitted character when practical.
- Add terminal punctuation when missing.
- Avoid doubled punctuation.
- Do not rewrite the phrase semantically.

Stored value:

```text
at the edge of the pit
```

Emitted value:

```text
At the edge of the pit.
```

Stored value:

```text
inside the doorway.
```

Emitted value:

```text
Inside the doorway.
```

---

## Conditional Emission

### World position and pose both present

Input:

```json
{
  "world_position": "at the edge of the pit",
  "pose": "Looking straight down into its depths.",
  "position": "center",
  "depth": "foreground"
}
```

Output:

```text
Tsaeytte: At the edge of the pit. Looking straight down into its depths. Place Tsaeytte in the center foreground region.
```

### World position present, pose absent

Input:

```json
{
  "world_position": "inside the doorway",
  "position": "right",
  "depth": "background"
}
```

Output:

```text
Rin: Inside the doorway. Place Rin in the right background region.
```

### Pose present, world position absent

Input:

```json
{
  "pose": "Raising both hands in warning.",
  "position": "right",
  "depth": "background"
}
```

Output:

```text
Rin: Raising both hands in warning. Place Rin in the right background region.
```

### Neither world position nor pose present

Input:

```json
{
  "position": "right",
  "depth": "background"
}
```

Output:

```text
Place Rin in the right background region.
```

### Camera placement incomplete

Preserve existing compiler behavior for missing `position` or `depth`.

Do not invent values.

Examples:

```text
Place Rin in the right region.
```

```text
Place Rin in the background region.
```

If the current compiler omits camera direction when either field is missing, retain that existing behavior rather than introducing a new policy as part of this change.

---

## Avoid Repetition

The compiler should avoid duplicate world-position statements when the same phrase also appears at the beginning of the pose.

Example input:

```json
{
  "world_position": "at the edge of the pit",
  "pose": "At the edge of the pit, looking straight down."
}
```

Preferred behavior:

- Do not attempt aggressive natural-language deduplication.
- Preserve both fields unless an exact normalized prefix match is detected.
- Optionally remove the duplicated exact prefix from the emitted pose.

Acceptable output:

```text
Tsaeytte: At the edge of the pit. Looking straight down. Place Tsaeytte in the center foreground region.
```

Do not add broad semantic rewriting in this change. Exact or near-exact prefix cleanup is sufficient.

---

## Backward Compatibility

Existing scene data such as:

```json
{
  "position": "center",
  "depth": "foreground",
  "pose": "At the edge of the pit. Looking straight down into its depths."
}
```

must continue to compile exactly as before.

Do not automatically migrate pose text into `world_position`.

A future migration tool may optionally separate known location phrases, but this task should not perform heuristic migration.

---

## Serialization

When saving a scene:

- Include `world_position` only when it contains non-whitespace text.
- Omit the property when blank.
- Preserve placement object ordering where practical.
- Recommended property order:

```json
{
  "element_id": "tsaeytte",
  "position": "center",
  "depth": "foreground",
  "world_position": "at the edge of the pit",
  "pose": "Looking straight down into its depths."
}
```

When loading:

- Missing field becomes an empty UI value.
- Unknown additional properties must remain unaffected.
- Existing files must not be rewritten merely because they were opened.

---

## Compiler Helper

Create or update a helper responsible for formatting one placement.

Suggested pseudocode:

```python
def format_placement(element_name, placement):
    world_position = normalize_sentence(placement.get("world_position"))
    pose = normalize_sentence(placement.get("pose"))
    camera = format_camera_region(
        element_name,
        placement.get("position"),
        placement.get("depth"),
    )

    parts = []

    if world_position:
        parts.append(world_position)

    if pose:
        parts.append(remove_exact_repeated_prefix(pose, world_position))

    if camera:
        parts.append(camera)

    if not parts:
        return None

    return f"- **{element_name}:** " + " ".join(parts)
```

Suggested camera formatter:

```python
def format_camera_region(element_name, position, depth):
    if position and depth:
        return f"Place {element_name} in the {position} {depth} region."
    if position:
        return f"Place {element_name} in the {position} region."
    if depth:
        return f"Place {element_name} in the {depth} region."
    return ""
```

Do not concatenate `world_position` directly between `position` and `depth`.

---

## Examples

### Tsaeytte

Scene data:

```json
{
  "element_id": "tsaeytte",
  "position": "center",
  "depth": "foreground",
  "world_position": "at the edge of the pit",
  "pose": "Looking straight down into its depths. Expression: excited."
}
```

Final prompt:

```text
- **Tsaeytte:** At the edge of the pit. Looking straight down into its depths. Expression: excited. Place Tsaeytte in the center foreground region.
```

### Freydis

Scene data:

```json
{
  "element_id": "freydis",
  "position": "right",
  "depth": "background",
  "world_position": "near the doorway in the far wall",
  "pose": "Looking directly at Tsaeytte. Expression: alarm."
}
```

Final prompt:

```text
- **Freydis:** Near the doorway in the far wall. Looking directly at Tsaeytte. Expression: alarm. Place Freydis in the right background region.
```

### Galen

Scene data:

```json
{
  "element_id": "galen",
  "position": "right",
  "depth": "background",
  "world_position": "beside Freydis near the doorway",
  "pose": "His free hand covers his forehead in exasperation."
}
```

Final prompt:

```text
- **Galen:** Beside Freydis near the doorway. His free hand covers his forehead in exasperation. Place Galen in the right background region.
```

### Environmental prop

Scene data:

```json
{
  "element_id": "utility-tusk",
  "position": "left",
  "depth": "foreground",
  "world_position": "on the stone floor behind Tsaeytte",
  "pose": "Its thick broken base drags along the floor while she holds the tapered end."
}
```

Final prompt:

```text
- **Utility Tusk:** On the stone floor behind Tsaeytte. Its thick broken base drags along the floor while she holds the tapered end. Place the Utility Tusk in the left foreground region.
```

---

## Tests

Add tests for schema, serialization, loading, and prompt compilation.

### Schema tests

- Placement accepts a valid string `world_position`.
- Placement accepts a missing `world_position`.
- Existing placement JSON remains valid.
- Blank `world_position` is treated as absent.

### Serialization tests

- Non-empty value is saved.
- Blank value is omitted.
- Loaded value round-trips without alteration other than outer whitespace trimming.

### Prompt compiler tests

#### Both fields present

Expected:

```text
Tsaeytte: At the edge of the pit. Looking straight down. Place Tsaeytte in the center foreground region.
```

#### Only world position present

Expected:

```text
Tsaeytte: At the edge of the pit. Place Tsaeytte in the center foreground region.
```

#### Only pose present

Expected:

```text
Tsaeytte: Looking straight down. Place Tsaeytte in the center foreground region.
```

#### Neither present

Expected:

```text
Place Tsaeytte in the center foreground region.
```

#### World position differs from camera position

Input:

```json
{
  "position": "left",
  "depth": "foreground",
  "world_position": "inside the doorway"
}
```

Expected:

```text
Tsaeytte: Inside the doorway. Place Tsaeytte in the left foreground region.
```

This test is important because it verifies that world-space and camera-space placement remain independent.

#### No merged location phrase

Assert that output does not contain forms such as:

```text
in the left doorway foreground
```

or:

```text
at the edge of the pit center foreground
```

---

## Acceptance Criteria

The change is complete when:

1. Placements support an optional `world_position` string.
2. The Scene Builder UI can edit and preserve it.
3. Existing scene files remain valid without migration.
4. The compiler emits world position separately from pose.
5. The compiler retains the existing `<position> <depth> region` camera direction.
6. World-space and camera-space placement are never merged into one phrase.
7. Blank values are omitted from saved JSON and final prompts.
8. Automated tests cover all combinations of world position, pose, position, and depth.
9. Existing prompt output remains unchanged for placements that do not use `world_position`.


## NPCs

## Expressions in Scenes

# Defer for now

zine maker: see Docs\Zines.md

.json inspector:
A page where I can select from a list of the key .json files and then view them in a table-like structure.

Setting up HTTPS://
