Implement WP17 from the Zet remediation plan:

`C:\Users\Joe\Projects\Zet\Docs\Zet reliability, library scaling, and model qualification.md`

Read the master plan's execution contract, the complete WP17 specification, and its dependencies, constraints, and acceptance criteria. Treat them as authoritative. Read any case table belonging to the package as part of its specification.

Primary repository context: ModelUpdater, Zet, and AI_Proxy only where this package requires changes.

Declared dependencies: WP13, WP16.

Before editing:

* inspect the current implementation and repository instructions relevant to WP17;
* read the completed dependency changes and handoffs, plus any other completed handoff affecting the code or contracts you will use;
* verify declared dependencies have passing acceptance evidence; if a required dependency is incomplete or failing, report the blocker and do not implement it inside WP17;
* identify any mismatch between the plan's assumptions and the repository;
* do not redesign the broader architecture unless implementation is blocked by a genuine contradiction;
* if a mismatch does not block WP17, implement the best plan-consistent solution and record the mismatch in the handoff.

Implement only WP17 and any minimal supporting changes required to satisfy its acceptance criteria.

Do not implement work assigned to later work packages. Preserve unrelated changes and existing authored content and queue tasks. Use one agent unless delegation is explicitly requested.

Package-specific guidance:

Inspect each repository's instructions and current state before editing. Use the existing AI_Proxy scheduler and queued worker; do not add a competing GPU scheduler. Trace whether direct Scene Builder calls bypass scheduling, and implement the package's queued routing only if that condition holds. Test family provenance, paired critic evidence, same-model reuse, cross-model unload, errors, and navigation under load. Read WP12's available performance evidence and limits, without claiming it has passed if no evidence exists. Use isolated test queues. Prepare any qualified critic assignment change for review; do not activate aliases or silently switch live assignments. Report changed files and test results separately for each repository.

Run the relevant existing tests and benchmarks, and add or adjust tests as needed to prove every WP17 acceptance criterion. Use the repository's verified Python 3 interpreter and existing tools. Do not install missing dependencies without approval. Review the final diff for unintended changes.

At completion, report:

* files changed;
* behavior changed;
* tests and benchmarks run, with commands and results;
* each WP17 acceptance criterion as PASS or FAIL, with evidence; label unavailable validation as FAIL (not verified), explaining why;
* any discovered issue that should modify or constrain a later work package.

Then update:

`C:\Users\Joe\Projects\Zet\Docs\Zet reliability, library scaling, and model qualification.md`

Add a `Handoff` section at the end of WP17, before the next work-package or top-level section, using exactly this structure:

Handoff:

* New invariants introduced:
* APIs/contracts changed:
* New tests/fixtures available to later WPs:
* Assumptions later WPs may rely on:
* Deviations from master plan:

Populate every item with concrete results from the implementation. If an item has no changes or deviations, state `None` rather than omitting it. If a WP17 handoff already exists, update it rather than appending a duplicate. Do not edit other package specifications or handoffs.

Include a concise acceptance result and any unresolved validation or blockers under `Assumptions later WPs may rely on:` so the next implementer can determine readiness. Do not describe an unverified assumption as an established invariant.

The remediation-plan document is the durable handoff record for subsequent work packages. Keep the handoff concise and factual; do not duplicate the full implementation report.

Do not begin WP18.

If Acceptance criteria and validations are all passed, then stage all changed files with a brief 1-2 sentence commit message.