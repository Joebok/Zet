Implement WP02 from the Zet remediation plan:

`C:\Users\Joe\Projects\Zet\Docs\Zet reliability, library scaling, and model qualification.md`

Treat the WP02 specification, dependencies, constraints, and acceptance criteria as authoritative.

Before editing:

* inspect the current repository implementation relevant to WP02 and the completed WP01 changes and handoff;
* identify any mismatch between the plan's assumptions and the repository;
* do not redesign the broader architecture unless implementation is blocked by a genuine contradiction;
* if a mismatch does not block WP02, implement the best plan-consistent solution and record the mismatch in the handoff.

Implement only WP02 and any minimal supporting changes required to satisfy its acceptance criteria.

Do not implement work assigned to later work packages.

Run the relevant tests and benchmarks, and add or adjust tests as needed to prove every WP02 acceptance criterion.

At completion, report:

* files changed;
* behavior changed;
* tests and benchmarks run, with results;
* each WP02 acceptance criterion as PASS or FAIL, with evidence;
* any discovered issue that should modify or constrain a later work package.

Then update:

`C:\Users\Joe\Projects\Zet\Docs\Zet reliability, library scaling, and model qualification.md`

Add a `Handoff` section at the end of WP02 using exactly this structure:

Handoff:

* New invariants introduced:
* APIs/contracts changed:
* New tests/fixtures available to later WPs:
* Assumptions later WPs may rely on:
* Deviations from master plan:

Populate every item with concrete results from the implementation. If an item has no changes or deviations, state `None` rather than omitting it.

The remediation-plan document is the durable handoff record for subsequent work packages. Keep the handoff concise and factual; do not duplicate the full implementation report.

Do not begin WP03.
