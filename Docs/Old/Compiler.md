# Compiler

## Deterministic Python Script to assemble the resources necessary to perform a <task>

## The output is a prompt to be handed over to an AI Agent.

## If any resources are missing

Then the <task> status must be updated.

## If all resources are found

Then the <task> is moved to it's next TaskState